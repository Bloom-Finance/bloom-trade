{
  "name": "@bloom-trade/ipfs",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/main.js",
  "module": "dist/main.js",
  "description": "A package entended to store encrypted data with dag-jose and DID instances",
  "repository": "https://github.com/Bloom-Finance/bloom-trade.git",
  "author": "Bloom Trade",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx ts-node src/main.ts",
    "start:sandbox": "node --experimental-specifier-resolution=node --loader ts-node/esm ./src/sandbox/test.ts",
    "build": "npx tsc",
    "lint": "eslint . --fix"
  },
  "devDependencies": {
    "@bloom-trade/types": "*"
  },
  "dependencies": {
    "dag-jose": "^4.0.0",
    "dids": "^3.4.0",
    "ipfs-core": "^0.18.0",
    "ipfs-http-client": "^60.0.0",
    "key-did-provider-ed25519": "^2.0.1",
    "key-did-resolver": "^2.3.0",
    "prom-client": "^14.1.1"
  }
}
